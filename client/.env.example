# Backend URL for production builds (leave unset in dev; Vite proxies /api).
# VITE_API_URL=https://your-matchday-api.onrender.com

# Optional: use Google Maps instead of the default OpenStreetMap embed.
# Create a key at https://console.cloud.google.com/ and enable "Maps Embed API".
# VITE_GOOGLE_MAPS_KEY=AIza...
